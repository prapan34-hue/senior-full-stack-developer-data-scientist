# Required only when the destructive reset endpoint should be enabled.
# Use a long, random value and never commit the real token.
DENGUE_ADMIN_TOKEN=replace-with-a-long-random-secret

# Optional: use a different SQLite database path.
# DENGUE_DB_PATH=C:\path\to\dengue.db
